# Extra autostart processes
# exec-once = uwsm-app -- my-service
# Disabled with the split-monitor-workspaces plugin (see bindings.conf).
# Re-enable to auto-load hyprpm plugins at startup.
# exec-once = hyprpm reload -n
exec-once = solaar -w hide
exec-once = hyprsunset
exec-once = ~/.config/hypr/scripts/kb-layout-osd.sh
